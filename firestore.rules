rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: open rules for debugging role fetch.
    // Replace with locked-down rules before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
